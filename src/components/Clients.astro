---
const clients = [
  { src: "images/img-clientes/friopacking.png", bg: "bg-gray-800" },
  { src: "images/img-clientes/danper.svg", bg: "bg-red-800" },
  { src: "images/img-clientes/pura-fruit.png", bg: "bg-gray-800" },
  { src: "images/img-clientes/savar.jpg" },
  { src: "images/img-clientes/boart-longyear.webp", bg: "bg-gray-500" },
  { src: "images/img-clientes/union.png", },
  { src: "images/img-clientes/servifabrick.png",  },
  { src: "images/img-clientes/logo_soltrak.png" },
  { src: "images/img-clientes/coreptec.svg"},
  { src: "images/img-clientes/overall_blue.svg"},
  { src: "images/img-clientes/frioteam.svg", bg: "bg-sky-950" },
];


---

<section class="py-[80px] md:py-[100px]">
  <div class="max-w-[1200px] mx-auto px-4 md:px-0">

    <div class="relative flex justify-between bg-[#00388B] py-5 md:py-10 px-3">

      <h3 class="relative text-start font-bold text-4xl md:text-5xl text-white">NUESTROS CLIENTES</h3>

    </div>

    <div class="flex items-center gap-2 md:gap-4 my-12">
      
      <button
        id="prevBtn"
        class="text-2xl md:text-3xl px-2 md:px-3"
      >
        ❮
      </button>

      <div
        id="carousel"
        class="flex gap-6 md:gap-12 overflow-hidden scroll-smooth w-full"
      >
        {
          clients.map((client) => (
            <div class={`flex items-center justify-center p-2 rounded-md shrink-0 ${client.bg ?? ""}`}>
              <img
                src={`${import.meta.env.BASE_URL}${client.src}`}
                alt="Cliente"
                class="h-10 md:h-12 lg:h-14 w-auto"
              />
            </div>
          ))
        }
      </div>

      <button
        id="nextBtn"
        class="text-2xl md:text-3xl px-2 md:px-3"
      >
        ❯
      </button>
    </div>
  </div>
</section>

<script>
  const carousel:any = document.getElementById("carousel");
  const nextBtn = document.getElementById("nextBtn");
  const prevBtn = document.getElementById("prevBtn");

  function getScrollAmount() {
    return carousel.clientWidth * 0.8; 
  }

  nextBtn?.addEventListener("click", () => {
    carousel.scrollBy({
      left: getScrollAmount(),
      behavior: "smooth",
    });
  });

  prevBtn?.addEventListener("click", () => {
    carousel.scrollBy({
      left: -getScrollAmount(),
      behavior: "smooth",
    });
  });
</script>
